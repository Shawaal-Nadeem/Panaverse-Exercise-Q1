import guestArray from "./q16";
for (let i=0;i<guestArray.length;i++)
{
    console.log(`You are inviting them to dinner ${guestArray[i]}`);
}
console.log('Total Number of Guests are : ',guestArray.length);
